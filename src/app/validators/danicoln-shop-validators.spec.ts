import { DanicolnShopValidators } from './danicoln-shop-validators';

describe('DanicolnShopValidators', () => {
  it('should create an instance', () => {
    expect(new DanicolnShopValidators()).toBeTruthy();
  });
});
